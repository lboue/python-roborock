# README

```bash
python -m venv venv
source venv/bin/activate
pip install aioresponses async_timeout construct pycryptodome pytest vacuum-map-parser-roborock paho-mqtt aiomqtt pyrate_limiter pytest-asyncio freezegun
```
